namespace HydraBusiness;

public interface ITemplateRepository
{
    // List<Category> Get();
    // List<Category> Get(int page, int pageSize, string name);
    // Category? Get(string id);
    // void Insert(Category req);
    // void Update(Category req);
    // void Delete(string id);
    // int CountData(string name);
}
